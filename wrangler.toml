#:schema node_modules/wrangler/config-schema.json
name = "factu-guide"
compatibility_date = "2026-05-22"

# Astro builds the static site into ./dist. Workers Static Assets serves it as-is.
[assets]
directory = "./dist"
not_found_handling = "404-page"
